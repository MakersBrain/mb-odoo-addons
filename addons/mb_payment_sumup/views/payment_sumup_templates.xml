<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- SumUp mints one URL per checkout and expects a plain GET to it, so the
         redirect form carries no fields at all. -->
    <template id="redirect_form">
        <form t-att-action="api_url" method="get"/>
    </template>

</odoo>
