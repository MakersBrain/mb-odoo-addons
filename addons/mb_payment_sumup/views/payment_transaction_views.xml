<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_form" model="ir.ui.view">
        <field name="name">payment.transaction.form.inherit.sumup</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <field name="provider_reference" position="after">
                <field name="sumup_transaction_id" invisible="provider_code != 'sumup'"/>
                <field name="sumup_checkout_url" widget="url" invisible="provider_code != 'sumup'"/>
                <field name="sumup_refund_target_amount"
                       invisible="provider_code != 'sumup' or operation != 'refund'"/>
            </field>
        </field>
    </record>

</odoo>
