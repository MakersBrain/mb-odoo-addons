# Commits that changed formatting only, and should be stepped over by blame.
#
# Enable locally once:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub reads this file automatically. Only add a reachable commit here if it
# is provably formatting-only. The original reformat commit was folded into a
# semantic squash, so there is currently no revision that is safe to ignore.
